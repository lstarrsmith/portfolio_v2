<h1> New Image </h1> <br>

Add new image

	<%= form_for [:admin, @project, @category, @image], url: {action: "create"}, :html => {:multipart => true} do |f| %>
		<%= f.label "Image file: " %> 
		<%= f.file_field :photo %>
		<%= f.text_field :caption %>
		<br>
		<%= f.submit "Submit", :class => "btn-primary" %>
	<% end %>